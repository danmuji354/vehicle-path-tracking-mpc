# Contributing

Controller changes must use the common plant, stay within declared input limits, add a deterministic scenario test, and report solver failures rather than silently substituting results.

